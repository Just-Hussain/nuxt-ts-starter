// https://github.com/nuxt-community/robots-module#options

// Typescript support is weird for this one, so it is not used.
export default [
	{
		Sitemap: `${process.env.BASE_URL}/sitemap.xml`,
	},
];
